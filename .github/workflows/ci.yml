name: FastAPI CI/CD

on:
  push:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Install uv
      uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
  
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.14'

    - name: Install dependencies
      run: uv sync
        
    - name: Run linting
      run: make lint

  test:
    runs-on: ubuntu-latest
    needs: lint

    steps:
    - name: Checkout code
      uses: actions/checkout@v6
    
    - name: Install uv
      uses: astral-sh/setup-uv@08807647e7069bb48b6ef5acd8ec9567f424441b
      with:
        enable-cache: true
    
    - name: Set up Python
      uses: actions/setup-python@v6
      with:
        python-version: '3.14'

    - name: Install dependencies
      run: uv sync
    
    - name: Run tests
      run: make test
    
  deploy-with-compose:
    runs-on: ubuntu-latest
    needs: [lint, test]
    steps:
    - name: Deploy with docker-compose
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.SERVER_HOST }}
        username: ${{ secrets.SERVER_USER }}
        key: ${{ secrets.SSH_PRIVATE_KEY }}
        script: |
          cd /opt/fastapi/TODO
          
          # Build new image
          docker build -t 11ghz/todo:1.0.0 .
          docker push 11ghz/todo:1.0.0
          docker rmi -f $(docker images -q)
          
          # Pull and deploy new image
          docker pull 11ghz/todo:1.0.0
          docker-compose down
          docker-compose up -d
          
          # Clean up old images
          docker image prune -f
    